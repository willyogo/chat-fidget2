"use strict";var e=require("node:crypto"),t=require("./crypto.js");function a(e){return e&&"object"==typeof e&&"default"in e?e:{default:e}}var r=/*#__PURE__*/a(e);describe("createHash",(()=>{it("should create a hash of the message using SHA256",(async()=>{let e="Hello, crypto!",a=await t.createHashBuffer(e);expect(a).toHaveLength(32);let o=Array.from(a),s=r.default.createHash("sha256").update(e).digest().toJSON().data;expect(o).toEqual(s)}))})),describe("randomBytes",(()=>{it("should create random bytes of the specified length",(()=>{expect(t.randomBytes(32).length).toEqual(32)})),it("should create different random bytes each time",(()=>{for(let e=0;e<20;e++){let[e,a]=[t.randomBytes(32),t.randomBytes(32)];expect(e).not.toEqual(a)}}))}));
